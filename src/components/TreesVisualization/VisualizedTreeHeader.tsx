import React from 'react';
import styled from 'styled-components';

// todo there will be header with zooming slider
const NoTreeHead = styled.h3`
  color: ${({ theme }) => (theme.color3)};
`;

const MainHeaderContainer = styled.div`
  display: flex;
  padding: 1em;
  margin-top: 1em;
`
